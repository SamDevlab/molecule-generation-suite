# Research OS 5.0.0

Status: **PASS** on `research-os-v1.3`.

Research OS 5.0.0 closes the v5 operational acceptance after a real, bounded top-level Live run. The release preserves the typed Research OS architecture, Oracle/ResearchQuestion workflow, fail-closed grounding, explicit Evidence hierarchy, provider-compatible structured output, deterministic semantic validation, frozen consistency support/signature, and scientific/security boundaries.

## Acceptance

- Attempt 8: `39/39 PASS`; no Attempt 9 is created.
- Reviewer panel: `PASS`.
- Final scientific exam: `PASS`.
- Follow-ups: `15/15 PASS`.
- Stress: `10/10 PASS`.
- Consistency: `5/5` pairs and `10/10` controlled calls `PASS`.
- Controlled calls used `CONSISTENCY` with `live_consistency.schema.json`.
- Cleanup: `PASS`; owned child processes remaining: `False`.
- Codex Evidence created: `0`.
- Codex EvidenceLevel changes: `0`.

Attempt 7 remains preserved as `37/39`, `PRIMARY_RECORD_DRIFT`, `BLOCKED_BEFORE_PASS`. It exposed the incomplete ID-only frozen basis. The completed full consistency signature freezes status, sorted grounded IDs, primary record, and sorted limitation codes; Attempt 8 then passed without changing historical artifacts.

## Scientific scope

The release does not claim experimental validation, clinical validation, universal generalization, or scientific truth. Computational results remain computational; physics simulations remain distinct from experiments; ML, database records, and Codex output do not become Evidence.

## Compatibility

The package version is `5.0.0`. Canonical Evidence levels remain exactly `E0_HEURISTIC`, `E1_ML`, `E2_COMPUTATIONAL`, `E3_PHYSICS`, `E4_CURATED_EXPERIMENTAL`, and `E5_VALIDATED_EXPERIMENTAL`.
